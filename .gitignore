node_modules/
.next/
out/
build/
.env
.env.local
.env.*.local
.DS_Store
*.log
npm-debug.log*
.vercel
next-env.d.ts
tsconfig.tsbuildinfo
*.tsbuildinfo

# Device-specific: the dev port here (3002) is chosen to avoid other projects
# already bound to 3000/3001 on this machine, so it should not be shared.
.claude/launch.json
